{
  "name": "leetcode-tracker-root",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --reporter=default --reporter=junit --outputFile=reports/results.xml",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.0.0",
    "@vitejs/plugin-react": "^6.0.1",
    "jsdom": "^26.0.0",
    "vite": "^8.0.4",
    "vitest": "^3.2.4"
  }
}
